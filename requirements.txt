# Faqat loyiha bevosita import qiladigan paketlar.
# Qolganlari (httpx, pydantic, APScheduler, lxml, primp...) shular orqali
# avtomatik o'rnatiladi — ularni bu yerga yozish shart emas.

# Telegram bot karkasi. [job-queue] — eslatmalar uchun APScheduler'ni tortadi.
python-telegram-bot[job-queue]==22.8

# AI model bilan gaplashish (OpenAI-mos har qanday provayder: Gemini, Groq, ...)
openai==3.14.0

# Ma'lumotlar bazasi (asinxron SQLite)
aiosqlite==0.22.1

# Internetdan qidirish — DuckDuckGo, kalit talab qilmaydi
ddgs==9.16.0

# .env faylini o'qish
python-dotenv==1.2.3

# Foydalanuvchi yuborgan fayllarni matnga aylantirish
pypdf==6.18.1          # PDF
python-docx==1.2.0     # Word (.docx)
openpyxl==3.1.5        # Excel (.xlsx)

# Vaqt mintaqalari bazasi — Windows va slim Docker image'da tizimda yo'q
tzdata==2026.4
